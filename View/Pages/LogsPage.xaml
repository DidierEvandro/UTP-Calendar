<Page
    x:Class="UTPCalendar.View.Pages.LogsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    mc:Ignorable="d">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Spacing="4" Margin="0,0,0,16">
            <TextBlock Text="Terminal de Sistema" Style="{ThemeResource TitleTextBlockStyle}"/>
        </StackPanel>

        <Border Grid.Row="1" CornerRadius="8" BorderThickness="1" BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}">
            <Border.Background>
                <AcrylicBrush TintColor="#0A0A0A" TintOpacity="0.75" FallbackColor="#1E1E1E" />
            </Border.Background>

            <ListView x:Name="TerminalListView" ItemsSource="{x:Bind ViewModel.LogMessages}" SelectionMode="None" Padding="8">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="x:String">
                        <TextBlock Text="{Binding}" Foreground="#F2F2F2" FontFamily="Cascadia Code, Consolas" 
                                   FontSize="13" TextWrapping="Wrap" Margin="0,2" IsTextSelectionEnabled="True"/>
                    </DataTemplate>
                </ListView.ItemTemplate>

                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="MinHeight" Value="0"/>
                        <Setter Property="Padding" Value="4,0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListViewItem">
                                    <ContentPresenter />
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListView.ItemContainerStyle>
            </ListView>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="10" Margin="0,12,0,0" HorizontalAlignment="Right">
            <Button Click="ClearLogs_Click">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <SymbolIcon Symbol="Delete"/>
                    <TextBlock Text="Limpiar terminal"/>
                </StackPanel>
            </Button>
            <Button Click="CopyLogs_Click" Style="{ThemeResource AccentButtonStyle}">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <SymbolIcon Symbol="Copy"/>
                    <TextBlock Text="Copiar historial"/>
                </StackPanel>
            </Button>
        </StackPanel>
    </Grid>
</Page>